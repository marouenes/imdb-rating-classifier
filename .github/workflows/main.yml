name: main

on:
  push:
    branches: [main, test-me-*]
  pull_request:

jobs:
  main-latest-git:
    uses: marouenes/workflows/.github/workflows/tox.yml@v1.2.0
    with:
      env: '["py37"]'
  main:
    uses: marouenes/workflows/.github/workflows/tox.yml@v1.2.0
    with:
      env: '["py38"]'
